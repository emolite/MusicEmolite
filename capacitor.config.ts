import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.songwithme.app',
  appName: 'SongWithMe',
  webDir: 'dist/MusicEmolite/browser'
};

export default config;
